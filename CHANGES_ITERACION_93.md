# V93
- Default anatomical source set to Z-Anatomy / BodyParts3D; manual generic mesh is no longer the primary workflow.
- Bundled `rig_map.json` for HALPE26 biplanar retargeting.
- Bundled attribution/provenance notice.
- Retarget payload bumped to v2 / app 93.0.
- UI explicitly distinguishes real anatomical mesh from real rigging/skinning; static GLB is never labelled as skinned.
- Preserves V90 angular kinematics and V92 OpenAtlas validation.
