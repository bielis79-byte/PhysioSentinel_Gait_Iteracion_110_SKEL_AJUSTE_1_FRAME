# V102 · Rig de paciente validable

Prueba primero `Rig solo` con `Paso +1` y `Reproducir`. Si el rig permanece conectado y reproduce una marcha coherente, usa `Rig + huesos` para comparar con el atlas fijo. V102 no anima todavía las mallas anatómicas: es deliberado para separar el fallo de cinemática del fallo de retargeting. `Grabar ciclo` genera un vídeo descargable del modo visual actual.
